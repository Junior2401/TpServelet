info:
  name: Liste organisateur
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:8081/organisateurs
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
