info:
  name: Liste tickets
  type: http
  seq: 17

http:
  method: GET
  url: http://localhost:8081/tickets
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
