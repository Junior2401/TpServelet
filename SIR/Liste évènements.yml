info:
  name: Liste évènements
  type: http
  seq: 15

http:
  method: GET
  url: http://localhost:8081/evenements
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
