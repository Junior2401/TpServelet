info:
  name: Créer ticket
  type: http
  seq: 12

http:
  method: POST
  url: http://localhost:8081/tickets
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
