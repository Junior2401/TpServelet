info:
  name: Liste types évènements
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8081/type_evenements
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
